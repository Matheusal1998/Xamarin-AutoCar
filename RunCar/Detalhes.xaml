<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" 
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             x:Class="RunCar.Detalhes"

             Title="{Binding Path=Veiculo.nome}">
  
        <StackLayout Padding="25">
            <TableView Intent="Settings">
                <TableRoot>
                    <TableSection Title="Acessórios">
                        <SwitchCell Text="{Binding TextFreio}" On="{Binding temFreioAbs, Mode=TwoWay}"></SwitchCell>
                        <SwitchCell Text= "{Binding TextAr}" On="{Binding temArCondionado, Mode=TwoWay}" ></SwitchCell>
                        <SwitchCell Text="{Binding TextMP3}" On="{Binding temMP3, Mode=TwoWay}"></SwitchCell>
                        <TextCell Text="{Binding ValorTotal}"></TextCell>
                    </TableSection>
                </TableRoot>
            </TableView>
                <Button x:Name="botaoProximo" Text="Próximo" Clicked="botaoProximo_Clicked"></Button>
              
        </StackLayout>
 
</ContentPage>